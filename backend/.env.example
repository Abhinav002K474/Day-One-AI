# ========================================
# DAY ONE AI - ENVIRONMENT CONFIGURATION
# ========================================

# ==================
# DATABASE CONFIG
# ==================
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=school_db
DB_PORT=3306

# NOTE: Use 127.0.0.1 instead of localhost for better stability
# Leave DB_PASSWORD empty if you haven't set a MySQL root password in XAMPP

# ==================
# SERVER CONFIG
# ==================
PORT=5000
NODE_ENV=development

# This is the port for the backend server
# Frontend uses relative paths, so no frontend port needed!

# ==================
# SECURITY
# ==================
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345
JWT_EXPIRY=24h

# IMPORTANT: Change JWT_SECRET to a random string in production!
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ==================
# AI SERVICES
# ==================

# Google Gemini API Key
# Get your free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative: If using old variable name (legacy support)
AI_API_KEY=your_gemini_api_key_here

# ==================
# FILE UPLOAD
# ==================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=150

# Max file size in MB for PDF uploads and question banks

# ==================
# CORS (Cross-Origin Resource Sharing)
# ==================
# CORS_ORIGIN=*
# By default, all origins are allowed
# In production, set this to your frontend domain:
# CORS_ORIGIN=https://yourdomain.com

# ==================
# OPTIONAL: External Services
# ==================
# EMAIL_SERVICE=smtp.gmail.com
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASSWORD=your_app_specific_password

# ==================
# DEPLOYMENT NOTES
# ==================
# For local development:
#   - DB_HOST=127.0.0.1 (XAMPP MySQL)
#   - PORT=5000
#   - NODE_ENV=development
#
# For production (e.g., Railway, Render):
#   - DB_HOST=<your-cloud-database-host>
#   - DB_USER=<your-database-user>
#   - DB_PASSWORD=<your-database-password>
#   - NODE_ENV=production
#   - Set all other variables in platform's dashboard

# ==================
# HOW TO USE
# ==================
# 1. Copy this file to `.env` in the same directory
# 2. Fill in your actual values (especially GEMINI_API_KEY)
# 3. Never commit `.env` to version control (already in .gitignore)
